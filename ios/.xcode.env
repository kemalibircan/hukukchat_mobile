# This `.xcode.env` file is versioned and is used to source the environment
# used when running script phases inside Xcode.
# To customize your local environment, you can create an `.xcode.env.local`
# file that is not versioned.

# NODE_BINARY variable contains the PATH to the node executable.
# This is required by React Native to work correctly with Xcode build process.
export NODE_BINARY=$(command -v node)

# Uncomment and customize the following examples based on your setup:

# For nvm users (with Homebrew):
# . "$(brew --prefix nvm)/nvm.sh" --no-use
# export NODE_BINARY=$(nvm which node)

# For specific Node version with nvm:
# . "$(brew --prefix nvm)/nvm.sh" --no-use
# nvm use 20.10.0
# export NODE_BINARY=$(command -v node)

# For users with manually installed Node versions:
# export NODE_BINARY=/path/to/your/node